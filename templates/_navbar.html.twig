<header>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand" href="{{ absolute_url('/') }}"><img src="{{ asset('img/WWJBlancL.png') }}"
                                                                    class="logo img-fluid"></a>
        <button class="navbar-toggler collapsed ml-auto" type="button" data-toggle="collapse"
                data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        <div class="navbar-collapse collapse" id="navbarColor01" style="">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('offer_index') }}"> Liste des offres</a>
                </li>
                <li class="nav-item">
                    {% if is_granted('ROLE_COMPANY') %}
                        <a class="nav-link" href="{{ path('offer_new') }}">Créer une offre</a>
                    {% endif %}
                </li>
                <li class="nav-item active">
                    {% if is_granted('ROLE_COMPANY') %}
                        <a class="nav-link" href="{{ path('company_profile') }}"> Mon profil</a>
                    {% elseif is_granted('ROLE_STUDENT') %}
                        <a class="nav-link" href="{{ path('student_profile') }}"> Mon profil</a>
                    {% elseif is_granted('ROLE_SCHOOL') %}
                        <a class="nav-link" href="{{ path('school_profile') }}"> Mon profil</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            </ul>
            {% else %}
                <ul class="navbar-nav ml-auto">
                    <li class="navbar-nav ml-auto">
                        <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </nav>
</header>
